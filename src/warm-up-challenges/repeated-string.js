const repeatedString = (n, s) => {
  // pass
};

const main = __ => {
  const n = parseInt(10);
  const s = 'aba';
  const result = repeatedString(n, s);
  console.log('Result:', result);
};

main();
